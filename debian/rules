#!/usr/bin/make -f
%:
	dh $@

DEB_HOST_ARCH := $(shell dpkg-architecture -qDEB_HOST_ARCH)

target = 

ifeq ($(DEB_HOST_ARCH),amd64)
	target = powder-64-sse3
else
	target = powder-sse3
endif

dh_auto_build:
	make $(target)
